info:
  name: hello
  type: http
  seq: 1

http:
  method: GET
  url: "{{base_url}}/hello"
  auth: inherit

runtime:
  variables:
    - name: base_url
      value: http://localhost:8080/api

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
