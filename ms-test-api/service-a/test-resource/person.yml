info:
  name: person
  type: http
  seq: 2

http:
  method: POST
  url: "{{base_url}}/person"
  auth: inherit

runtime:
  variables:
    - name: base_url
      value: http://localhost:8181/api

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
