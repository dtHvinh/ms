info:
  name: "Health "
  type: http
  seq: 1

http:
  method: GET
  url: "{{a}}/health"
  auth: inherit

runtime:
  variables:
    - name: base_url
      value: http://localhost:8181/api

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
